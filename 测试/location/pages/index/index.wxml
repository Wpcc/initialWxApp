<!--index.wxml-->
<view class="container">
  <button bindtap="openSetter">设置</button>
  <map longitude="{{longitude}}" latitude='{{latitude}}' markers="{{markers}}" show-compass="{{compassTrue}}"></map>
</view>

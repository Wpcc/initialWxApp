<!--pages/recharge/index.wxml-->
<view class='container'>
  <view class="container-box">
    <van-button primary="primary" wx:for="{{money}}" wx:key="{{item.id}}" id="{{index + 1}}" bind:click="onCharge">{{item[index + 1]}}元</van-button>
  </view>
</view>
